export enum CreateArgument {
    MAIN = "main",
    LANDING_PAGE = "landing-page",
    APP = "app",
    DATABASE = "database",
    TABLE = "table",
    COLUMN = "column",
    RELATION = "relation",
}
